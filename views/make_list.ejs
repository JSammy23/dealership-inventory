<h2><%= title %></h2>
<div class="row">
    <div class="col">
        <% if(make_list && make_list.length > 0) { %>
            <ul>
                <% make_list.forEach(make => { %>
                    <li>
                        <a href="<%= make.url %>"> <%= make.name %></a>
                    </li>
                <% }) %>
            </ul>
        <% } else { %>
            <p>No Makes available.</p>
        <% } %>
    </div>
</div>